package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium High-Density White, Black, and Blue Palette
val ExecutiveBlue = Color(0xFF005BC1)        // Theme Brand Primary Blue (#005BC1)
val LightBlueAccent = Color(0xFFD7E3FF)      // High Density Soft Blue container background (#D7E3FF)
val ElectricBlue = Color(0xFF005BC1)         // Theme Accent Blue
val DeepSlateBlue = Color(0xFF001B3F)        // On-Blue container dark text (#001B3F)

val PureWhite = Color(0xFFFFFFFF)            // Pure White surfaces
val OffWhite = Color(0xFFF8F9FF)             // Theme Off-white bluish background (#F8F9FF)
val DarkSlateBorder = Color(0xFFE1E2EC)      // Soft High Density border color (#E1E2EC)

val MatteBlack = Color(0xFF1A1C1E)           // Deep charcoal/black text (#1A1C1E)
val SlateTextDark = Color(0xFF44474E)        // Secondary Slate dark text (#44474E)
val SlateTextLight = Color(0xFF74777F)       // Tertiary Slate light text

// Lavender accents from High Density specifications
val LavenderContainer = Color(0xFFF3E2FF)    // Soft Purple container background (#F3E2FF)
val DeepLavenderText = Color(0xFF2D005D)     // On-Purple container dark text (#2D005D)

// Functional Colors
val PositiveGrowth = Color(0xFF15693B)       // Green for positive numbers / stock
val WarningAlert = Color(0xFFBA1A1A)         // Red for errors / low stock
val PendingOrange = Color(0xFFF59E0B)        // Orange for pending invoices
val ProgressiveOrange = PendingOrange        // Orange for progress / in-transit notes
